/**
 * @file serverUtils.tsx
 * @description Utilidades específicas para ServerSelectorScreen
 */

import { normalizeColor } from "../../../../utils/serverUtils";

// Re-export de utilidades necesarias
export { normalizeColor };

// Nota: Se removieron helpers no usados (calculateScrollPadding, getHeaderTitle)
